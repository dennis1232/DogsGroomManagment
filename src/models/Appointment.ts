export type Appointment = {
  id: number;
  customerId: number;
  petName: string;
  petSize: string;
  appointmentTime: string;
  groomingDuration: number;
  customerName?: string;
  createdAt: string;
};
